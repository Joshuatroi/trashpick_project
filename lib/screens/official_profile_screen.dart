import 'package:flutter/material.dart';

class OfficialProfileScreen extends StatelessWidget {
  const OfficialProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Official Profile Screen'),
    );
  }
}
