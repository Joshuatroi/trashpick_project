import 'package:flutter/material.dart';

class OfficialRoutes extends StatelessWidget {
  const OfficialRoutes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Official Routes Screen'),
    );
  }
}
